/*
  Skeleton of a component
    * imports (optional)
    * component defn 
       must return JSX
    * export 
*/
// Functional Component with Named Function
function Header() {
  // must return JSX
  return (
    <div>my header</div>
  )
}

export default Header;